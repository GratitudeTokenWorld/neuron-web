node_modules/
dist/
*.log
.DS_Store
coverage/
.vitest/
.vite/
.certs/
.relay-data/
*.local

# Relay runtime state — secrets/biometrics + archives, never commit.
# Broad glob covers every .relay-* file incl. .relay-generation.json and the
# atomic-write .tmp/.bak sidecars.
.relay-*
reload.log
