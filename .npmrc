# Supply-chain guard: only install package versions published ≥30 days ago
# (npm ≥11.6 `min-release-age`, unit = DAYS, numeric only). A fresh malicious
# release can't enter the tree — locally or on the relay boxes (file is committed).
# Explicit `--before` on the CLI still overrides for deliberate pins.
min-release-age=30
